const calculator = {
    add: function(x, y) {
        return x + y;
    },
    minus: function(x, y) {
        return x - y;
    },
    multiply: function(x, y) {
        return x * y;
    },
    divided: function(x, y) {
        return x / y;
    },
}

export {calculator};